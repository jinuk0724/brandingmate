name: Threads auto publish

on:
  schedule:
    # 09:00, 16:00, 20:00 Asia/Seoul
    - cron: "0 0,7,11 * * *"
    # One-off test run: 2026-05-29 22:00 Asia/Seoul
    - cron: "0 13 29 5 *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Generate only, do not publish"
        required: true
        default: "true"
        type: choice
        options:
          - "true"
          - "false"

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
      OPENAI_MODEL: ${{ vars.OPENAI_MODEL || 'gpt-5-mini' }}
      THREADS_MAX_CHARS: "500"
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Check schedule guard
        id: schedule_guard
        shell: bash
        run: |
          SHOULD_RUN=true
          if [ "${{ github.event_name }}" = "schedule" ] && [ "${{ github.event.schedule }}" = "0 13 29 5 *" ]; then
            if [ "$(date -u +%Y-%m-%d)" != "2026-05-29" ]; then
              SHOULD_RUN=false
            fi
          fi
          echo "should_run=$SHOULD_RUN" >> "$GITHUB_OUTPUT"

      - name: Generate and publish Threads post
        if: steps.schedule_guard.outputs.should_run == 'true'
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.dry_run }}" = "true" ]; then
            node scripts/generate_and_publish_threads_post.js --dry-run
          else
            node scripts/generate_and_publish_threads_post.js
          fi

      - name: Upload generated post
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: latest-threads-post
          path: scripts/latest-post.txt
          if-no-files-found: ignore
